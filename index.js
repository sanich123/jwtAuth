const { start } = require("./init.js");
// const { router } = require("./auth-router.js");
// const { Routes } = require("./const.js");
// const PORT = process.env.PORT || 3000;
// const app = express();

// app.use(express.json());

// app.get("/", (req, res) => {
//   res.send("Hello world");
// });
// app.use(Routes.auth, router);

start();

// module.exports = app;
